{
  "name": "auto-pop-monorepo",
  "version": "1.0.0",
  "description": "auto-pop monorepo",
  "author": "RuariM",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^1.9.0"
  }
}
